{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "nest-module-generator",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Module name",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    }
  },
  "required": [
    "name"
  ]
}