/**
 * The configuration interface of the NewrelicModule
 */
export interface NewrelicOptions {
  /**
   * config if newrelic module is register as global module. default is true.
   */
  global?: boolean;
}
